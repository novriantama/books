# books
How to run
1. copy .env.sample to .env, then change the env variable
2. run "make deps" in command prompt
3. run "make tidy" in command prompt
4. run "make run" in command prompt
5. open postman and import the postman json collection